/// 날짜/시간 유틸리티
///
/// 캘린더 앱 전반에서 사용하는 날짜 헬퍼 함수들
///
/// TODO: 구현할 함수 목록
///   - isSameDay(a, b) → bool
///   - getDateRange(center, viewType) → (start, end)
///   - formatMonthYear(date) → String ("April 2026")
///   - formatTime(dateTime) → String ("2:30 PM")
///   - formatDateRange(start, end) → String
///   - getWeekNumber(date) → int
///   - getThreeDayRange(date) → List<DateTime>
///   - roundToNearest15Min(dateTime) → DateTime
///   - durationBetween(start, end) → Duration
